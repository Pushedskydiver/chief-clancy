# Progress

Living state document for the Clancy monorepo. Records the current state, the phase ledger, and the next decision. Session-by-session detail lives in git history (each phase's PRs are tagged + commit messages reference them).

## Current state (2026-04-15)

**Local-init-flow complete.** Full local pipeline shipped end-to-end: `/clancy:init` (board-optional) → `/clancy:brief` → `/clancy:plan --from` → `/clancy:approve-plan` (writes `.approved` marker) → `/clancy:implement --from` (single-plan runs without marker check; batch filters by marker existence; SHA verification deferred). All four PRs (#288, #289, #290, #291) merged. Changeset will publish `@chief-clancy/dev` patch and `@chief-clancy/terminal` patch on next publish workflow run.

**Published versions:** (versions bumped by changeset merge — publish workflow pending)

| Package                  | Version |
| ------------------------ | ------- |
| `@chief-clancy/core`     | 1.0.0   |
| `@chief-clancy/terminal` | 0.2.1   |
| `@chief-clancy/dev`      | 0.4.1   |
| `@chief-clancy/brief`    | 0.4.3   |
| `@chief-clancy/plan`     | 0.7.1   |
| `@chief-clancy/scan`     | 0.2.3   |
| `chief-clancy` (wrapper) | 0.9.26  |

**Test counts:** 879 core, 755 terminal, 1201 dev, 126 brief, 326 plan = **3287 total**.

**Last shipped:** §P3 execution-mechanics rules promotion PR [#314](https://github.com/Pushedskydiver/chief-clancy/pull/314) in Session 86 (2026-04-15). Three rules moved from Barrier pilot notes into repo docs per `docs/DEVELOPMENT.md §P3`'s n=2 meta-criterion: **§Flatten-boundary intra-wrapper grep**, **madge cycle-baseline diff**, **name-collision audit for bulk sed** — full walks in `SELF-REVIEW §Folder structure`, mandate gates in `DA-REVIEW §Folder structure`. Restart-from-manifest stays n=0 pending real-world reproduction. 2 Copilot rounds (R1 × 2 findings, R2 × 1 ERE-vs-BRE cascade from R1 fix; all accepted). Preceded by Barrier-Core PR [#312](https://github.com/Pushedskydiver/chief-clancy/pull/312) + version-packages PR [#313](https://github.com/Pushedskydiver/chief-clancy/pull/313) — first public-npm semver-major via the Barrier pattern: `@chief-clancy/core` bumped `0.2.0 → 1.0.0` (first 1.0 in the ecosystem), `@chief-clancy/dev` patch, `@chief-clancy/terminal` patch, `chief-clancy` patch via `workspace:*` cascade. 5 commits (PR #312), 176 files, +451/-733. 34 single-impl wrapper folders flattened, 38 internal `index.ts` barrels deleted; only `packages/core/src/index.ts` remains under `core/`'s src tree.

**Session 78 (research-only, no PRs):** Option A research phase complete. Four-round peer-review gauntlet (ML/SWE/DA lenses × 3 rounds + focused SWE on §11) surfaced 29 → 8 → 3 → 6 findings; exit criterion met. All evidence + final design at `.claude/research/option-a/` (gitignored). Key outcomes: three-layer framing collapsed to dual-mode (grounding + code-review) with write-time prevention deferred as bounded follow-on; invocation mechanism identified as `PreToolUse` agent-hook (novel-in-repo, spike as Step 0c); docs-maintenance reduced to three docs-as-tests (~11% corpus, low cost).

**Session 79 (Step 0 execution + Wiring-claim audit):** 64-tuple corpus reconstructed (PRs #291/#278/#283) + hardened DA run against 35 in-gate + 5 FP samples. End-to-end recall **65.7% (23/35)**, gate PASS (+15.7pp above 50% threshold). FP rate 0/5 real (1 novel catch). Fix-overcorrection subset weaker at 43% — addressed in PR #294 (Wiring-claim direction audit). **Decision: ship DA-hardening only** — PRs #293 + #294 are the entire Option-A deliverable. All Step 1+ work (subagent, LSP, mechanism spike, agent-hook) cancelled. Full protocol + results at `.claude/research/option-a/eval/step-0-results.md`. Session 79 handoff triggered (2 PRs + heavy Option-A context).

**Session 80 (long teaching session, no PRs):** **Readability review.** Captured Alex's house style as 13 evidence-backed code rules + 6 process rules at `.claude/research/readability/notes.md`. North star: fresh-reader cognitive load. Multiple research rounds (Fowler, Martin, Beck, Ousterhout, Pocock, Vanderkam, Cockburn, Evans, Karpathy autoresearch, Klein premortem, Goodhart/Strathern, etc.) backing each rule with citations and honest caveats. Three parallel grill-subagents tested the ruleset; surfaced a fabricated premise (Rule 11's "CLAUDE.md says no comments" — that text is from the global Claude Code system prompt, not the project file) and a major reversal not previously acknowledged (Rule 7 inverts active barrel-maintenance practice in CONVENTIONS.md + SELF-REVIEW.md, including the Phase 8 audit's "11 missing barrels" finding). Notes now reframe both as honest policy proposals.

**Session 81 (research + planning, no PRs):** **Phase 3 of readability handoff complete — all 8 decisions closed.** Consolidated 6 P-rules → 3 (old P2 + P6 deleted with rationale, old P3 merged into P1). Key outcomes: (1) core exports → middle-option subdirectory wildcards (`./types/*.js` etc.); (2) "grilling" kept, `GLOSSARY` collision resolved by scoping; (3) P-rule ownership via Claude Code FileChanged hooks + CI gates + ad-hoc; (4) P1 pre-grill at rule-adoption not per-run; (5) state-surface per-field ownership (not precedence); (6) Rule 12 `error: string` → tagged discriminated union `{kind,...}` with `UnknownError` escape hatch, opportunistic migration; (7) Rule 7 reversal evidence tightened (IDE invalidation + coupling, not tree-shaking); (8) **promotion plan v7 locked after 7 iterative grill rounds** — author's "done" claim was wrong 3 times in a row; each round found real defects. Also surfaced + corrected: CLAUDE.md line 71 cites `arxiv:2602.11988` (typo; real paper is Lulla et al. `arxiv:2601.20404`) AND summarises it inverted (paper shows AGENTS.md _improves_ agent efficiency, not reduces). `@chief-clancy/core@0.1.2` confirmed public on npm (3 versions); PR4 is a real 0.2.0 semver break. New load-bearing lessons: (a) light-pass grilling after substantive changes is not theatre — it finds real bugs; (b) P1 extends to PR-level specs for non-trivial PRs. All work in `.claude/research/readability/notes.md`.

**Session 82 (2026-04-14/15) — PR-1 #295 + PR0 #296 + PR #297 + PR1 #298 + PR #299 + PR2 #300 + PR3 #301 + PR4 #302 — promotion plan v7 opens and runs through all doc + code-surface PRs:** Eight PRs shipped across one extended working session. Four plan PRs (PR-1, PR0, PR1, PR2), two promotion-plan PRs with package-surface changes (PR3, PR4), and two pilot-driven spin-offs (PR #297 SELF-REVIEW tightening, PR #299 DA-REVIEW tightening). **PR-1 (CLAUDE.md philosophy correction):** fixed broken `arxiv:2602.11988` citation (real paper is Lulla et al. 2026 `arxiv:2601.20404`) and removed the inverted "non-minimal reduces task success" claim; added three honest citations — Lulla 2026 (presence benefit), Levy et al. 2024 `arxiv:2402.14848` (size degradation), Liu et al. 2023 `arxiv:2307.03172` (U-shaped recall). Converted bare doc pointers to Pattern-D explicit triggers at lines 67, 73-74, and 82 (Key-docs block). MEMORY.md rationale updated out-of-band (auto-memory, not repo-tracked); MEMORY.md content restoration flagged as a separate, non-blocking workstream. **PR0 (`docs/PROMOTION-PLAN.md` pointer + checklist + PR-1 follow-up):** added ephemeral pointer file — pointer-only per Decision 5 anti-duplication (does NOT copy plan content from `notes.md`). Includes merge-status table (PR-1 ✅ #295 landed; PR0 → PR6 tracked), per-PR housekeeping checklist, concurrent-modification embargo, and grilling cadence reminder. Deleted once PR6 lands. **DA-pilot iteration 1 findings rolled in:** (a) docs/CONVENTIONS.md:182 + docs/RATIONALIZATIONS.md:99 still cited the fabricated `arxiv:2602.11988` paper and the inverted "reduce task success ~3%" claim (PR-1 post-restructure sweep miss — B1); both sites rewritten with the honest Levy 2024 + Liu 2023 triad, with RATIONALIZATIONS.md retaining a historical note that the original trim's rationale was fabricated. (b) PROMOTION-PLAN.md grilling-cadence overclaim ("7 consecutive rounds") corrected to "every grill round through v7 … stopping-claim wrong on 3 rounds in a row" per notes.md §1620-1629 ground truth (M1). **Copilot review then caught four issues both DA iterations missed:** broken GitHub link to gitignored notes.md (fixed: plain backtick path + local-workspace clarifier); PR0 row missing self-link in merge-status table (fixed: #296 linked); duplicate "Session 82" label in PROGRESS.md (fixed: entries merged into this single session entry — Copilot's suggested rename to Session 83 was factually wrong since both PRs landed in the same working session); bare `CONVENTIONS.md:182` / `RATIONALIZATIONS.md:99` inconsistent with `docs/` prefix used elsewhere (fixed: prefixed). **Pilot meta-finding (captured in notes.md):** DA iteration count is the wrong dial — same-prompt iterations wouldn't have caught these. Load-bearing lessons: (i) DA prompt needs reader's-perspective + structural-completeness checks; (ii) DA prompt needs intra-file consistency check; (iii) self-review was skipped on PR-1 and retrofit on PR0 still missed the path-prefix issue — self-review itself needs a "grep every file reference; are the paths consistent?" check. **Three-layer review-gate split now has local evidence each layer catches what the others miss** — DA (architecture/claims), Copilot (structural reachability + internal consistency + style), self (meta/process honesty). **Spin-off PR #297 (docs/SELF-REVIEW.md tightening):** three new Consistency-section bullets encoding the Copilot catches per `docs/SELF-REVIEW.md:12`'s living-document rule — link reachability from the GitHub-web reading context, intra-file path-prefix consistency, grep-anchor uniqueness within a file. **PR1 (GLOSSARY.md additions):** Spec grilling entry, package-export/dependency boundary pair, Public API reconciliation (canonical package-level definition; CONVENTIONS.md looser senses reconcile when Rule 11 promotes). Four Copilot rounds: DA iter-1 caught 2 BLOCKING (fabricated quote, overstated forward-ref) + 2 MATERIAL (one being path-prefix violation of PR #297's own new rule — **convergent evidence the tightening transferred**); Copilot surfaced 2 new classes (stale-forward via PR-plan vocabulary, tool-semantics on Node exports). **Spin-off PR #299 (docs/DA-REVIEW.md tightening):** extended Behaviour-claim bucket with external-tool semantics + stale-forward regex with `added by PR|promoted in PR|when Rule \d+|after PR \d+ lands` + PR-plan-vocabulary red-flag paragraph. **PR2 (CONVENTIONS.md Error Handling):** Rule 12 tagged-union Result shape with current-shape baseline documenting the heterogeneous state today; first PR to show the **nit-floor transition** (R3 British spelling nit → R4 silent). **PR3 (DEVELOPMENT.md Process rules + State-surface ownership):** P1/P2/P3 ownership model + per-field state-surface table + anti-duplication rules + 2 new Pattern-D triggers in CLAUDE.md. DA iter-1 caught 3 BLOCKING all "aspirational-as-fact" (FileChanged hook + CI gate + focus.md convention all presented as wired when nothing existed); Copilot R1 caught that `FileChanged` isn't a real Claude Code hook event (it's `PostToolUse`); Copilot R2 flagged "workstream directory" undefined — **first PR to warrant research**, dispatched P1-style spec-grilling subagent with adversarial framing, pushed back toward root `focus.md` with migration rule to `workstreams/<id>/focus.md` on concurrency, citing Karpathy `program.md` prior art + State-surface doctrine consistency + YAGNI. **PR4 (core exports tightening, first code-surface PR):** 0.1.x → 0.2.0 semver break on public npm; namespaced subpath wildcards (`./types/*.js`, `./schemas/*.js`, `./shared/*.js`, `./board/*.js`); DA iter-1 much lighter than doc-only PRs — hypothesised cause: external validators (`publint` + `attw` 🟢 across Node10/16-ESM/CJS/bundler) cover structural verification. Notes §1771 had listed brief/plan/chief-clancy as needing changesets but grep confirmed only terminal+dev depend on core — corrected enumeration. Third nit-floor data point (R1 prose → R2 silent). **Pilot log catch-up (notes.md):** systematic per-PR entries now exist for all 8 PRs, with 12 numbered gap classes (4 encoded in SELF-REVIEW + DA-REVIEW, 7 queued, 1 meta), 7 meta-findings, and 5 refined open questions. **Handoff trigger:** 8 PRs exceeds the 3-PR threshold; PROGRESS.md updated before next session. Context is heavy — recommend fresh session for next workstream.

**Session 83 (2026-04-15) — PR #305 Barrier shipped clean (first-of-kind P3 supervised):** One PR, but the entire session was the pilot. **Plan survived 6 grill rounds** before any code moved (v1 self-grill → v2 verification → v3 external subagent → v4 external subagent → v5 external subagent → R6 external **verification-framed** grill with explicit confirm-or-disprove brief). Convergence curve R1=4 → R2=0 → R3=4 → R4=5 → R5=2 → R6=0 (confirmed). Each of the 4 commits had its own DA discovery loop + quality-suite gate; final verification DA on the combined diff returned "READY TO MERGE." PR merged with **zero Copilot comments**. **Manifest:** 67 internal `index.ts` barrels deleted (46 dev + 21 terminal), 57 single-impl wrapper folders flattened (38 dev + 19 terminal), 1 edge case (`runner/dep-factory/index.ts` → `dep-factory.ts`), 10 multi-content/name-mismatch folders kept intact. `core/` completely untouched (wildcard exports locked by PR #302). Combined diff 237 files, +387/-673. **Governance sweep landed in commit 4:** `.github/copilot-instructions.md:49` reworded, `.github/pull_request_template.md:46` swapped barrel-checkbox for `src/index.ts` checkbox, `knip.json` terminal glob updated, 5 `docs/**/*.md` deep-path refs updated for flatten-target moves. **Mid-run stop-the-lines (5 caught, all resolved before merge):** (a) over-broad sed `/foo/foo.js` → `/foo.js` corrupted `core/schemas/github/github.js` and `core/schemas/gitlab/gitlab.js` — fixed before commit 3; (b) same sed false-matched `lifecycle/feasibility/feasibility.js` (multi-content, not a flatten target) — fixed; (c) `deliver-phase.test.ts` had `vi.mock('../pipeline/index.js', …)` surviving commit 1 since static grep missed string-literal mock paths — caught at test-run, fixed; (d) DA flagged 2 docs refs using trailing-slash form (`runner/autopilot/`) my regex missed — fixed in commit 4; (e) madge showed 3 cycles but all pre-existing on `main` — captured as separate cleanup workstream, NOT a Barrier regression. **Pilot meta-findings (captured in `.claude/research/barrier/notes.md`):** (i) two-phase grill discipline (discovery R1-R5 + verification R6) is different in kind, not just "another round" — the last discovery round has self-terminating bias since agent + author both want to converge; an evaluative-skeptical R6 with confirm-or-disprove framing catches what discovery converges past; (ii) per-commit DA + final per-PR verification DA both pull weight — per-commit catches mechanical errors where blast radius is one commit, final catches integration-level issues; (iii) "verification rounds can legitimately return zero" is a real pattern — don't misread R_n=0 as "done" unless it's the explicit verification phase; (iv) last-segment sed patterns are dangerous when flatten-target names collide with unrelated subpath folders — either scope to full path or audit cross-package subpaths before bulk sed; (v) `vi.mock` and dynamic `import()` paths are invisible to static import grep — must grep them explicitly as a separate pass before running tests; (vi) docs sweep regex must cover both `X/X.ts` deep-path refs AND bare `X/` directory refs; (vii) madge "stop-the-line on cycles" should mean NEW cycles (regressions), not absolute — plan rule text needs clarification. **Blame preservation confirmed** — all 96 file renames across commits 3+4 detected by git (similarity 88-100%). **Version bumps from the PR4 exports break + Barrier:** `@chief-clancy/core@0.2.0`, `@chief-clancy/dev@0.4.0`, `@chief-clancy/terminal@0.2.0`, `chief-clancy@0.9.25` published. **Handoff now** (Option B per Alex): preserve fresh-context independence for PR5 drafting and for the P3 rule promotion, which is the separate end-of-run-triage workstream.

**Session 84 (2026-04-15) — promotion-plan v7 closes + P3 rule promoted + self-review scope clarified — 6 PRs shipped:** The three queued workstreams from Session 83's handoff (PR5, P3 promotion, Barrier-Core) all progressed. Barrier-Core remains queued (dual anchor intact); everything else closed out. **PR5 ([#306](https://github.com/Pushedskydiver/chief-clancy/pull/306)):** Rule 7 + Rule 11 rewrite in `docs/CONVENTIONS.md` shipped as planned — 5-round grill stack (R1-R4 discovery + R_n verification) on the spec, then per-commit DA + final verification DA on the diff. Copilot surfaced 10 findings across three review passes; 8 were the same class (absolute-phrase-contradicts-another-section-of-same-diff). All resolved. 6 commits total. **PR5-housekeeping ([#307](https://github.com/Pushedskydiver/chief-clancy/pull/307)):** 1-commit merge-status table update + PR6 scope additions codifying the cross-section-consistency rule the Copilot findings surfaced. **PR6 ([#308](https://github.com/Pushedskydiver/chief-clancy/pull/308)):** SELF-REVIEW + DA-REVIEW per-rule line-items for Rule 7 + Rule 11 + new `### Multi-section internal-consistency pass` subsection. 3 discovery grills + 1 verification grill + per-commit DA + final verification DA + 3 Copilot rounds. Every review stage found self-referential violations of the PR's own new rules — convergent evidence the discipline is load-bearing. 5 commits. **PR6-housekeeping ([#309](https://github.com/Pushedskydiver/chief-clancy/pull/309)):** Deleted `docs/PROMOTION-PLAN.md` per its own lifecycle ("deleted once PR6 lands"), swept one stale DA-REVIEW citation that referenced it. First real application of PR6's Cross-doc consistency sweep rule — worked on the triggering commit. **P3 promotion ([#310](https://github.com/Pushedskydiver/chief-clancy/pull/310)):** Partial promotion to `docs/DEVELOPMENT.md`. A research subagent disproved two earlier drafts (iterative-DA-to-nit-floor, multi-PR coordination DA) with actual PR evidence before the final plan was drafted. What shipped: §P1 patched to reference new §Two-phase grill discipline subsection (shared mechanism — discovery R1..R_n-1 + verification R_n with confirm-or-disprove brief + "verification round must actually fire" binding clause); §P3 gained `#### Review discipline` subsection (plan-stage grill + per-commit DA + final verification DA + "two passes, not N — Copilot is the third reviewer" + cross-section-consistency check trigger + link-don't-restate for mechanical-refactor checklist + pending-n=2 placeholder for execution-mechanics rules). Clean on second Copilot pass — first time in the series. **Self-review scope clarify ([#311](https://github.com/Pushedskydiver/chief-clancy/pull/311)):** Three validation rounds disproved successively larger drafts (PR-type split → unified-with-commit-message-rule → unified-with-audit-trail-subsection) until landing on the minimal version the evidence supports: one sentence added to `SELF-REVIEW.md:37` clarifying that DA's Multi-section pass covers one §Consistency bullet (absolute phrases + adjacent supersession footnote drift); every other section remains author-owned. Addresses Alex's concern on PR #310 that "self-review done inline" claims weren't naming sections walked. **Meta-arc signal:** three-round adversarial validation with strong-evidence briefs (research subagents given PR comment history + git log + file greps) disproved increasingly smaller drafts until reaching minimal. Works. Expensive in tokens, cheap in defects-reaching-main. **Direct-to-main commits:** `f0ca16b` Barrier-Core schedule record (pre-PR5, per `feedback_progress_md_direct_to_main` memory). No others this session.

**Session 85 (2026-04-15 — research-only, no PRs, no commits) — Barrier-Core plan grilled to nit-floor.** Defaults validated, scope ground-truthed, plan drafted and iterated through three discovery rounds + one verification round. Four plan drafts (v1 → v4) at `.claude/research/barrier-core/` (gitignored). **Defaults-validation subagent** (pre-draft) disproved the handoff's "cycles-after" framing (all 3 cycles live in dev+terminal, not core — `madge --circular packages/core/src` = 0) and corrected cross-package site count (handoff said 43, actual 28 — grep-verified). Also surfaced changeset-coupling, `export type` preservation (Barrier pilot R4 F#5), and Phase-F timing anchor as load-bearing considerations. **Scope ground-truth** (`.claude/research/barrier-core/scope-ground-truth.md`): 38 barrels to delete, 33 single-impl wrappers to flatten, ~181 intra-core + 28 cross-package rewrites, 37 X/X.ts collision candidates, zero `vi.mock`/dynamic-import sites referencing deleted barrel paths. Critical finding: Barrier-Core has **two mechanical operation classes** (not one like Barrier-dev/terminal) — (A) per-symbol barrel deletion for 11 fan-out barrels + (B) bulk-sed flatten for 33 wrappers + a new Class C (package-entry `src/index.ts` cascade discovered by R1: it re-exports from **every** deleted barrel, making it the single largest rewrite target, spanning commits 2-4). **Grill arc:** R1 surfaced 11 findings (2 BLOCKING: package entry + `board/<provider>/index.ts` mid-commit-3 breakage). R2 surfaced 9 findings (2 BLOCKING same-class: `../sibling/` relative imports + `vi.mock()` string literals not covered by sed anchors — led to new §Flatten-boundary discipline). R3 surfaced 2 real BLOCKING (alias-axis flatten-boundary gap on `~/c/`, symmetric to R2) + 3 fabricated MAJOR findings (R3 confused barrel files with package-entry file — verified with `grep -c "from './types/index.js'" core/src/index.ts` = 2, not the 4 R3 claimed). R_n verification (evaluative-skeptical, "confirm or disprove" brief) cleanly disproved R3's fabrications AND caught one real MATERIAL (azdo worked-example in v4 table used `…` placeholder where real symbol split needed — patched in-place). **Strong validation of two-phase grill discipline:** discovery + verification produce different signals; verification with evidence access catches fabrications discovery converges past. **Novel rule discovered:** §Flatten-boundary discipline (rewrite intra-wrapper relative + alias cross-sibling imports + `vi.mock`/`import()` string-literal paths atomically before rmdir). Candidate for DEVELOPMENT.md §P3 promotion **pending Barrier-Core's execution validating it (n=2 for execution-mechanics rules)**. No code changes or commits this session; Barrier-Core execution-ready at plan-v4.

**Session 86 (2026-04-15) — Barrier-Core shipped.** PR [#312](https://github.com/Pushedskydiver/chief-clancy/pull/312) merged clean (1 Copilot prose-nit on R1 accepted + applied as `5291532`, clean R2). Version-packages automation: PR [#313](https://github.com/Pushedskydiver/chief-clancy/pull/313) — `@chief-clancy/core@1.0.0` (first 1.0), `@chief-clancy/dev@0.4.1`, `@chief-clancy/terminal@0.2.1`, `chief-clancy@0.9.26`. **Manifest:** 6-commit sequence per plan-v4 (`.claude/research/barrier-core/plan-v4.md`, gitignored — read as delta on plan-v3): 1 schemas flatten (8 wrappers) → 2 shared flatten (9 wrappers, 9 barrels) → 3 board flatten (17 wrappers + §Flatten-boundary compound transforms + azdo/notion per-symbol splits against multi-content api barrels) → 4 fan-out barrel deletion + Class-A per-symbol rewrites across types/schemas/shared-http/board/6-provider/2-api barrels + Class-C package-entry cascade + 27 cross-package rewrites → 5 governance sweep (CONVENTIONS Migration state + Barrels table + 7 other docs + 2 changesets + .github surfaces) → 6 verification (no code change). Final PR diff: 176 files, +451/-733. **Per-commit DA: zero blocking across all 5 commits.** Final verification DA: READY TO MERGE (cross-doc internal-consistency pass triggered by commit 5 — 6 surfaces walked, all coherent). **Stop-the-lines captured in `notes.md`:** (a) plan-v2 commit-1 prose "7 re-export lines" was actually 12 (azdo untouched — plan conflated re-exports-from-7 with 7-all-flattening); (b) first-run `pnpm test` flake reproduced 5 times, always clean on re-run, confirmed infrastructure not Barrier-Core; (c) plan "11 fan-out barrels" was actually 12 (azdo/api + notion/api barrels added to commit 4 — ground-truth list had 38 items, 9+17+12=38); (d) **commit-4 types/remote.js heuristic miss** — first perl pass enumerated remote-family symbols from memory (`GitPlatform|RemoteInfo`), missed `PrCreation*`/`PrReviewState`/`*Remote`; build caught 11 TS2305 errors; fixed with second-pass slurp-mode perl using full remote.ts export list. Meta-lesson: **for N-target per-symbol discrimination, always enumerate from source file, not from memory.** **§Flatten-boundary discipline is now n=2 validated** (pilot PR #305 n=1; Barrier-Core n=2) — ready to promote to DEVELOPMENT.md §P3 as the n=2 execution-mechanics rule. **Honest commit-sizing reflection:** commits 1/2/5 right-sized; commit 3 at the edge but defensible (single "board subtree flatten" theme); commit 4 too big in retrospect — 78 files, +260/-479 — should have been split 4a (types/schemas/shared-http non-board fan-outs) + 4b (board + 6 provider + 2 api barrels), cleaner single-theme reviews + isolated blast radius for the stop-the-line. Filing as "commit-sizing is a grill-time question, not just plan-shape question". **Process misses flagged on PR:** (i) branch named `barrier-core` not `chore/barrier-core` per CLAUDE.md conventions; (ii) amended commit 5 with single-sentence NIT fix violates `docs/GIT.md` no-amend rule. Both prose-only / low-stakes; noted, left in place. **Total DA signal across 2 Barrier runs = 0 mechanical findings:** two-phase grill + per-commit gates pre-resolve the defect classes DA targets; DA is walking behind, not ahead. Worth a doctrine discussion.

**Session 86 addendum (2026-04-15) — §P3 rules promotion shipped same day as Barrier-Core.** PR [#314](https://github.com/Pushedskydiver/chief-clancy/pull/314) merged. Two-phase grill applied to the promotion spec itself (meta: can't skip grilling a PR that promotes the grill). Discovery surfaced 2 MATERIAL (over-claimed "zero consumer-path errors" — commit-4 had 11 TS2305 errors, different rule class; R_n catch mis-attributed to §Flatten-boundary — R_n actually caught a plan-v4 azdo placeholder) + LOW glosses for "compound-transform"/"commit-fate"; both MATERIAL resolved with honest scoping. Verification round returned READY TO MERGE. Copilot R1 caught (a) regex-vs-literal ambiguity in the six grep patterns + (b) PR-ref convention drift (fixed: `[Barrier pilot]` → `Barrier pilot (PR #305)`). Copilot R2 caught an ERE-vs-BRE cascade from the R1 fix — my "copy-paste verbatim" claim was wrong without tool specification; clarified patterns are for `rg` / `grep -E`. **Meta-reinforcement:** 3 Copilot findings across Barrier-Core + §P3 PRs were all reader-experience / convention-consistency class — the exact class pre-execution grill + DA systematically don't catch. Three-reviewer stack is genuinely layered, not redundant.

**Next (Session 87+):** **(1) Pre-existing cycles workstream** — 3 dev/terminal cycles (`dev/src/dep-factory/deliver-phase.ts ↔ dep-factory.ts`; `dev/src/lifecycle/rework/rework-handlers.ts ↔ rework-builders.ts`; `terminal/src/installer/install/install.ts ↔ validate-sources.ts`). No plan grilled yet; Alex-scheduled, independent. **(2) PR7 notes-file deletion** — deferred from readability-rule-promotion series. **(3) Phase F (`@chief-clancy/design`)** — Stitch integration, first new package since Phase E. Post-Barrier-Core surface stable at 1.0.0 so design's first deep imports target the 1.0 shape. **Open doctrine thread:** DA returned zero mechanical findings across both Barrier runs (PR #305 + #312, 11 per-commit DA passes + 2 final verification passes = 13 passes, 0 findings). Pre-execution grill pre-resolves the defect classes DA's checklist targets; Copilot catches what grill + DA systematically miss. Either the stack working as designed or DA has drifted to confirmation-gate-not-discovery-gate. Worth examining when bandwidth — could be a DA-REVIEW §Required disciplines retune or a prompt-shape change. Not urgent. Full iteration history at `.claude/research/{barrier,barrier-core,readability,p3-promotion,self-review-split}/` (gitignored).

## Phase ledger

| Phase                                              | Status                 | Shipped       | Headline                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| -------------------------------------------------- | ---------------------- | ------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **A** — Monorepo rebuild                           | ✅ done                | 2026-03-31    | Two packages (core + terminal), internal capability boundaries enforced by eslint-plugin-boundaries                                                                                                                                                                                                                                                                                                                                                                                                              |
| **B** — Plan extraction + local planning           | ✅ done                | (pre-Phase-C) | `@chief-clancy/plan` standalone, `--from <brief>` flow, local plans in `.clancy/plans/`                                                                                                                                                                                                                                                                                                                                                                                                                          |
| **C** — Plan approval gate                         | ✅ done                | 2026-04-09    | SHA-256 `.approved` marker, standalone-aware Step 1, optional board push from approve-plan, PR 8 deferred to dev                                                                                                                                                                                                                                                                                                                                                                                                 |
| **D** — Brief absorbs approve-brief                | ✅ done                | 2026-04-09    | Strategist directory deleted, virtual-role transition, install-mode preflight, Step 6 label-decision preamble                                                                                                                                                                                                                                                                                                                                                                                                    |
| **Docs lifecycle update**                          | ✅ done                | 2026-04-09    | RATIONALIZATIONS.md + DA-REVIEW Required disciplines + Severity Labels + Prove-It Pattern + Stop-the-Line + CLAUDE.md                                                                                                                                                                                                                                                                                                                                                                                            |
| **Post-research trim**                             | ✅ done                | 2026-04-09    | CLAUDE.md trimmed 10 → 4 bullets per AGENTS.md paper, CONVENTIONS.md "Output style" added per Brevity Constraints paper, GIT.md No --amend, memory pruned 8 files                                                                                                                                                                                                                                                                                                                                                |
| **E** — `dev` extraction + AFK executor            | ✅ done                | 2026-04-12    | Standalone dev package with readiness gate, AFK loop, artifact writers, cross-package install/update/uninstall system                                                                                                                                                                                                                                                                                                                                                                                            |
| **Local-init-flow** — board-optional path          | ✅ done                | 2026-04-14    | Init board gate, settings/doctor/help/autopilot/status/review local-mode awareness, comprehensive docs sweep. PRs #288, #289, #290, #291. Batch mode filters by `.approved` existence; SHA verifier deferred                                                                                                                                                                                                                                                                                                     |
| **Option A research** — pre-PR reviewer            | ✅ done (discontinued) | 2026-04-14    | Step 0 gate PASS (recall 65.7% on 35 retrieval-addressable tuples). Decision: DA-hardening only; Option A subagent cancelled. PR #293 (Claim-extraction pass) is the deliverable. Artefacts: `.claude/research/option-a/eval/step-0-results.md`                                                                                                                                                                                                                                                                  |
| **Readability rule promotion** (promotion-plan v7) | ✅ done                | 2026-04-15    | 14 PRs across Sessions 81-84: PR-1 CLAUDE.md philosophy + PR0 PROMOTION-PLAN pointer + PR1 glossary + PR2 Rule 12 + PR3 DEVELOPMENT process rules + PR4 core exports (0.2.0) + Barrier (#305, first-of-kind P3) + PR5 Rule 7/11 + PR5-housekeeping + PR6 SELF/DA line-items + PR6-housekeeping (delete PROMOTION-PLAN) + #297/#299 review-gate tightening spin-offs + PR #310 P3 promotion + PR #311 self-review scope. PR7 notes-file deletion deferred post-Phase-5.                                           |
| **P3 rule promotion**                              | ✅ done (partial)      | 2026-04-15    | Two-phase grill discipline codified in DEVELOPMENT.md §P1 (shared mechanism); P3 §Review discipline expanded with per-commit DA + final verification DA + cross-section-consistency trigger (PR #310). Execution-mechanics rules (madge cycle-baseline diff, name-collision audit, restart-from-manifest) deferred to post-Barrier-Core-run (n=2 validation). Iterative-DA-to-nit-floor and multi-PR coordination rules proposed and disproved with evidence — explicitly NOT promoted.                          |
| **Barrier-Core** — `core/` Rule 7 + 11 migration   | ✅ done                | 2026-04-15    | PR [#312](https://github.com/Pushedskydiver/chief-clancy/pull/312) + version-packages PR [#313](https://github.com/Pushedskydiver/chief-clancy/pull/313). `@chief-clancy/core@1.0.0` (first 1.0). 34 single-impl wrapper folders flattened, 38 internal barrels deleted; only `packages/core/src/index.ts` remains. 6 commits, 176 files, +451/-733. Per-commit DA zero-blocking; final verification DA READY TO MERGE; 1 Copilot prose-nit R1 (accepted) → clean R2.                                            |
| **§P3 execution-mechanics rules promotion**        | ✅ done                | 2026-04-15    | PR [#314](https://github.com/Pushedskydiver/chief-clancy/pull/314). Three rules moved from Barrier pilot notes into `docs/SELF-REVIEW.md` + `docs/DA-REVIEW.md` §Folder structure per `docs/DEVELOPMENT.md §P3`'s n=2 meta-criterion: §Flatten-boundary intra-wrapper grep (n=1; promoted on rule-emergence + clean-execution evidence), madge cycle-baseline diff (n=2), name-collision audit for bulk sed (n=2). Restart-from-manifest stays n=0. Two-phase grill on the spec; 2 Copilot rounds both accepted. |

Detail for all phases lives in git history. Disciplines are documented in `docs/DEVELOPMENT.md`, `docs/DA-REVIEW.md`, `docs/TESTING.md`, `docs/CONVENTIONS.md`, and `docs/RATIONALIZATIONS.md`.

## Build order (remaining packages)

1. ~~`@chief-clancy/brief`~~ — done (Phase A)
2. ~~`@chief-clancy/plan`~~ — done (Phase B + C)
3. ~~`@chief-clancy/dev`~~ — done (Phase E)
4. `@chief-clancy/design` — Phase F (Stitch integration)
5. `@chief-clancy/cli` — interactive wizard
6. `@chief-clancy/chat` — conversational interface (Slack/Teams)

See [`docs/decisions/architecture/package-evolution.md`](docs/decisions/architecture/package-evolution.md) for the full rationale.

## Repo

https://github.com/Pushedskydiver/chief-clancy
