/**
 * Dev runtime entry point — no-op stub for `clancy-dev.js` bundle.
 *
 * Replaced with real pipeline wiring once the single-ticket executor
 * is implemented. Exists so the esbuild config produces a working bundle.
 */
console.log('clancy-dev v0.0.0 (stub)');
