{
  "name": "@chief-clancy/terminal",
  "version": "0.1.0",
  "description": "Installer, slash commands, hooks, AFK runner, agents, Claude CLI bridge",
  "author": "Alex Clapperton",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "dependencies": {
    "@chief-clancy/core": "workspace:*"
  },
  "scripts": {
    "build": "tsc --project tsconfig.build.json && tsc-alias --project tsconfig.build.json && node dist/hooks/esbuild.hooks.js",
    "test": "vitest run",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.27.4"
  }
}
